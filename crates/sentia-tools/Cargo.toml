[package]
name = "sentia-tools"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Deterministic read-only Linux tool implementations for Sentia"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
