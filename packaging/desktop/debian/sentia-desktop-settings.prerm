#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        update-alternatives --remove desktop-background \
            /usr/share/backgrounds/sentia/sentia-wallpaper.svg
        ;;
esac

#DEBHELPER#

exit 0
